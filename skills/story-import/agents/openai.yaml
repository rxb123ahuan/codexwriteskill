interface:
  display_name: "小说导入"
  short_description: "把已有小说整理成项目结构"
  brand_color: "#2F6F5E"
  default_prompt: "Use $story-import to convert my existing novel into a structured writing project."

policy:
  allow_implicit_invocation: true